# Checkpoint Record 004 — TASK-2026-09-15-v010-ms6-cli (session boundary: S4+S5 → sign-off)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` · **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` §3.4
- **Checkpoint type**: Session boundary (slice-scoped GO: this instance authorized S4 incl. gate→commit→push per handoff-003 §Scope, plus S5 doc-sync)
- **Branch / Revision**: `main` @ S4 feat `f357584` + this boundary commit (push + headSha CI watch at tip)
- **Timestamp**: 2026-09-15 09:26 UTC (`date -u`)

## State

- **MS-6**: `in_progress` · #6 assigned · M2: MS-1…5 ✅ · **S1 ✅ S2 ✅ S3 ✅ S4 ✅ S5 ✅ / sign-off open**
- **Suite**: **319/319** four-stage gate green this session (18 new tests over S3's 301)
- **Milestone position**: after S4+S5 the CLI ships the full v0.1.0 surface (init · route · state(get/list/transition/requirement add/evidence add) · verify · workflow(list/validate/import)); remaining is maintainer sign-off, then MS-7 (#7)

## Completed this session (S4, plan-001 §3 + S5 doc-sync)

- `verify <TASK>|--all` — engine cold path (`loadContract`/`buildEnv`/`verifyTask`), exits 0/1/2/**12**; contract discovery reuses the S3 binding ruling; `--all` scans `verifying` tasks, empty = ok true exit 0 (recorded, not spec'd); CLI logs `verify.run` only — no auto-transition (safest cli-reference reading, recorded)
- `workflow import <path>` — JSON pack file-or-dir import (MS-8 owns v1 Markdown); conservative stub contract, `command`/`command_fails` never copied, `skipped_commands` reported; exits 0/2/3; `workflow list` unchanged (packs stay typed constants)
- **PASS-and-BLOCK proof** (AC-5): per gate exercised through the CLI surface — file_exists PASS then BLOCK on the same task via the built bin; real timeout (`sleep 5`, `timeout_ms: 1000` → exit 12); import strips a `command` check and proves the stub carries none
- Async dispatch: `CommandRunner` returns `Envelope | Promise<Envelope>`; `run()` awaits (top-level await in entry only — AGENTS-allowed); no unit test imports `run`, golden spawn unaffected
- **S5 doc-sync** (AC-6): cli-reference scope note (implemented vs deferred past v0.1.0), init scaffold-only Creates (4 state files, no packs/MS-8, no briefing/MS-7), doctor 4-pack line (R1), `state requirement add` section added, `state claim` marked deferred, `state get` md example fixed, verify exits 0/1/2/12 + binding + log-only + `--all`-empty notes (undeclared `--json`/`--no-cache` rows removed), workflow import/validate rewritten to S4 semantics; verification-guide exits + timeout-wins + `timeout_ms` bounds; getting-started init tree (4 state files) + exit-12 row. errors.md: no change — every S4 code already catalogued
- Budgets (AC-3, for the record): root help 64/200, verify 16/40, workflow 30/40 words — asserted by help.test.ts every gate
- Stale honesty tests repaired: S2/S3-era "unwired" asserts flipped to wired S4 behavior (2 golden); family-runner await in 2 unit tests (async `CommandRunner` type)

## Decisions & invariants (bind the next session)

- Unchanged locked set plus S4 rulings (record §Scope Change S4): verify log-only; `--all`-empty exit 0; stub shape (safe checks travel, commands counted-not-copied, empty-safe fallback is `evidence_exists` on kind `import`); dir import requires `done.schema.json`
- `state claim` stays out (RFC §3.4 closed set); cli-reference now marks all deferred sections explicitly
- No new error codes in S4 — catalog reuse is a finding, not an omission (errors.md §Adding an Error Code requires a test per code; none was needed)
- Read-bytes-before-write + same-step provenance; `date -u`; silence ≠ authorization; per-instance push naming; headSha-asserted CI watch; gates never read through pipes without PIPESTATUS

## Anomalies & lessons (this session — for NOTES §6 mining)

1. **Sync CLI vs async engine**: `run()` was sync, `verifyTask` is async. Options were spawnSync-duplication (logic fork, FMEA smell) vs async `run()`. Chose async — safe because no unit test imports `run` (verified by grep before changing) and golden tests spawn the bin. Lesson: check the import graph before fearing a signature change.
2. **ESLint `no-useless-assignment` on `let stat = null; try { stat = ... }`**: restructured to `let isDir: boolean` assigned once in try. The rule fires on never-read-before-reassign — declare late, not early.
3. **Prettier⇄edit loop again (5th milestone)**: batched `--write` immediately before the final gate; gate stayed green.
4. **Two golden honesty tests failed post-wire** (expected — they asserted S3's "unwired" surface). Repaired to assert wired behavior, not deleted: honesty tests must track the registry, and S4's wiring is exactly what they now prove.

## Blockers

- None mechanical. Open gate: **maintainer sign-off on MS-6** (AC-1…AC-8 review) before `completed` + #6 close. S5 sign-off checklist for the maintainer is in handoff-004 §Scope.

## Exactly one prioritized next action

**Maintainer review → MS-7** (`…ms6-cli.handoff-004.md`): sign-off checklist (AC-1…AC-8 with evidence pointers), then `completed` + close #6. No code remains for MS-6.
