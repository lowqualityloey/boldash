# Checkpoint Record 002 — TASK-2026-09-15-v010-ms6-cli (session boundary: S2 → S3)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` · **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` (§3.4 = v0.1.0 surface)
- **Checkpoint type**: Session boundary (slice-scoped GO: this instance authorized S2, incl. its commit+push per handoff-001 §Scope)
- **Branch / Revision**: `main` @ S2 feat commit `01c1154` + this boundary commit (next push, CI-watched at tip)
- **Timestamp**: 2026-09-15 03:39 UTC (`date -u`)

## State

- **MS-6**: `in_progress` · #6 assigned · M2 position: **MS-1…MS-5 ✅ · MS-6 S1 ✅ S2 ✅ / S3…S5 open · MS-7…MS-9 planned**
- **Suite**: **271/271** four-stage gate (`verify` = lint → format:check → typecheck → test), green at 03:37 UTC this session; CI status for `01c1154` + boundary commit recorded in the task record after push

## Completed this session (S2, plan-001 §3)

- `state get <id>` / `state list --status --risk --type --owner --limit` (read-only; unknown id → `STATE_TASK_NOT_FOUND` exit 2 confirmed; uninitialized repo → `CLI_PRECONDITION_FAILED`; corrupt state surfaces engine errors — fail-closed, never silent-empty)
- `workflow list` (exactly the R1 four-pack set in registry order) / `workflow validate <name>` → exit 0/2/3
- **`workflow validate` semantics resolved** (handoff-001 gap-1 decision, now in record §Scope Change): registry-entry structural validation (SCHEMA_VALIDATION) then mandatory-capability check against the generic floor (CAPABILITY_MISSING, exit 3 — unprobed ⇒ unavailable); on-disk manifest/done-schema wording deferred honestly to MS-8 + S5 docs patch
- Bare-family runners (`state`, `workflow` → CLI_USAGE naming wired subs) without touching the S1 kernel
- `src/core/state/index.ts` re-export barrel (plan §4.1 import hygiene); S1 deep-import rewired
- help.test.ts now walks subcommands for the ≤40 budget and asserts root-honesty BOTH ways (unwired absent, wired present)
- +42 tests: unit (state runners incl. owner-filter via fixture state; validateWorkflowPack PASS sides — all four built-ins + optional-beyond-floor stays advisory — and 10 BLOCK cases) + golden spawn of the built bin (envelopes, exits, byte-stability, stdout data schema-validated vs task.schema.json)

## Remaining work (MS-6)

1. **S3**: `route` (`--input/--json/stdin`, `--create`) + `state transition` / `requirement add` / `evidence add` — includes the **AC-8 file-baseline obligation** at `planned→implementing` (path + sha256 for every bound `must_not` path) and route envelope token budgets (≤80 success / ≤140 failure, deterministic serializer test)
2. **S4**: `verify TASK|--all` (exit 12 incl.) + `workflow import` + per-gate PASS-and-BLOCK evidence (AC-5)
3. **S5**: docs sync (AC-6: cli-reference :125, validate wording, init scaffold-only, `--format md` example contradiction below), final full-surface budgets, record AC fill → maintainer sign-off → #6 close

## Decisions & invariants (bind the next session)

- P1–P10 + tie-breaker · ADRs 0001–0005 · ajv@8.20.0 SOLE runtime dep · exact pins, ESM, strict TS (`exactOptionalPropertyTypes` — conditional spread for optional props; `noUncheckedIndexedAccess`)
- Command honesty rule: registry carries only wired surface — S3 must NOT pre-register `state transition` before it runs, etc.
- RFC §3.4 CLOSED command set: `claim`/`doctor`/`explain`/`checkpoint`/`diff`/`evidence` families in cli-reference stay unregistered (ADR-0005 trims); `state claim` is NOT in plan-001 §2 — do not build it
- **New S2 precedent — doc-internal contradiction found**: cli-reference §state get examples show `--format md` but §Output Contract + argv kernel allow only `human|json`. Raised, not papered over: S5 either patches the example or an ADR lands; until then `md` is a `CLI_USAGE` exit 2
- Read bytes before write + post-write both-direction asserts; `date -u`; silence ≠ authorization; per-instance push naming; CI watch asserts headSha
- Gates: four stages; corpus prettier-clean at every commit

## Anomalies & lessons (this session — for NOTES §6 mining)

1. **Stale resume contract, again**: this session opened with a briefing claiming MS-6 unclaimed / GO-MS6 pending / expect 187/187 — disk said GO given 01:24, S1 done, 229/229. The four-line contract executed honestly (pull/verify/gh) and the mismatch was surfaced, not coded over. Resume briefings are hypotheses; STATE.md + records are the evidence.
2. **BLOCK-side fixture caught by an earlier guard**: capability test's synthetic pack still had `subagents` in `optional`, so the structural requires∩optional guard correctly blocked first — test fixed to truth (checkpoint-001 lesson 5 recurs); guards firing in designed order is proof of fail-closed layering, not a bug.
3. `lint` stage rejected ANSI-matching regex (`no-control-regex`): assert absence with string `toContain('\u001b[')`, not a control-char regex.
4. `exactOptionalPropertyTypes` + enum flags: resolve validated values with `values.find(v => v === raw)` — typed membership with zero casts.
5. Prettier `--write` invalidates edit-tool freshness every time it touches a file — re-read-then-edit loop (checkpoint-001 lesson 7, third recurrence, now three-gate-deep).
6. Every ID/time/count line above was executed or copy-pasted this step (lesson 8 of checkpoint-001 stands).

## Blockers

- None. S3→S5 need no human input until S5 completion sign-off; `--format md` and any `workflow validate` wording objection are maintainer-visible via record §Scope Change.

## Exactly one prioritized next action

**S3** (plan-001 §3): build `route` (proposal from `--input`/`--json`/stdin through the router's public surface; envelope budgets asserted deterministically) with `--create` writing a `planned` task via the State Engine, then `state transition` + `requirement add` + `evidence add` — the transition to `implementing` must capture **file-baseline evidence (path + sha256) for every bound `must_not` path** (AC-8): before coding, read `schemas/done.schema.json` + `src/core/verification/{checks,gate,evidence}.ts` to find how `file_not_modified` resolves its evidence, so the capture format is proven against the consumer, not invented.
