# Handoff Record 003 — DSH session (MS-6 S3) → fresh session (S4)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` (in_progress) · **Next slice**: S4 per plan-001 §3
- **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` §3.4 · **Checkpoint**: `…ms6-cli.checkpoint-003.md`
- **Revision to validate against**: `origin/main` ≥ S3 feat `c539c56` + boundary commit

## Receiver validation checklist (complete BEFORE any edit)

- [ ] `git pull` · `git log --oneline -6` (top: S3 `feat(cli)` + this boundary commit); `git status` clean — records beat briefings; surface stale premises, never code over them
- [ ] `npm ci --cache /tmp/boldash-npm-cache && npm run verify` → exit 0, **301/301** (four stages; never read a gate through a pipe without `${PIPESTATUS[0]}`)
- [ ] Read order: `AGENTS.md` → handoff-003 + checkpoint-003 → `docs/STATE.md` → MS-6 record → **plan-001 §3 S4 verbatim** → `docs/cli-reference.md` §verify + §workflow import → `docs/verification-guide.md` §Gates and Exit Codes (0/1/2/12 incl. timeout amendment) → `src/core/verification/{index,gate,contract,command}.ts` → `src/cli/commands/state-writes.ts` (binding pattern to reuse)
- [ ] `gh issue view 6` — claimed, in_progress; #15 (SECURITY email) maintainer-only
- [ ] Recite: P1–P10 · ajv-only · registry honesty (S4 wires verify + workflow import ONLY when runnable) · binding ruling: task contract = `.boldash/workflows/<task.workflow>/done.schema.json` · read-bytes-before-write + same-step provenance · silence ≠ authorization · per-instance push naming · `date -u`

## Scope accepted by receiver

S4 only (plan-001 §3): `verify <TASK>|--all` → verification engine cold path (`getValidator`/`loadContract`/`buildEnv`/`verifyTask`), exits 0/1/2/**12** — timeout implemented per errors.md here (§3.4 amendment); `--all` scans `verifying` tasks (engine list filter already supports it). AC-5 per-gate PASS-and-BLOCK evidence exercised **through the CLI surface**. Then `workflow import <path>` per the plan's exact words — schema-validated pack import (exit 0/2/3). Remember: **v1/PromptKit importer is MS-8 (#8)**, and core never parses YAML (plan invariant 2) — pin S4's pack shape from the done-schema meta-schema + cli-reference, record the resolution honestly, do not silently expand scope. After: four-stage gate → commit → push only under per-instance naming → headSha-asserted CI watch → record/STATE updates.

## Known gaps the receiver must NOT paper over

- verify on a task whose contract is missing: engine returns VERIFY_CONTRACT_INVALID — map to documented exit 2, don't invent "skip"; `--all` with zero verifying tasks: verify what cli-reference actually says (empty = exit 0 or a usage error?) — if undefined, pick the minimal honest behavior and record it
- exit 12 must be a REAL timeout (short `timeout_ms` on a declared `command` check; `command.ts` enforces 300 s default) — prove the gate blocks AND times out, both directions
- events: verify success → CLI persists `verify.run` event + `verifying→complete`/`failed` transition (gate.ts header says CLI owns this; engine never writes state) — decide whether `verify` mutates state or requires an explicit `state transition`; cli-reference §verify shows NO transition side effect ⇒ safest reading: verify only logs `verify.run`, humans/agents transition — record whichever you implement
- budgets: verify's LLM-facing envelope has no documented cap yet — root help ≤200 still holds (verify adds one line); record actual token counts of golden envelopes in the record so S5 can assert budgets
- AC-6 doc-sync pile-up grows: cli-reference claim row, `--format md` example, :125 pack list, verify exit-12 row — none implemented silently
- npm cache hazard, Prettier⇄freshness loop, PIPESTATUS trap — all in checkpoint-003 §Anomalies
