# Handoff Record 004 — DSH session (MS-6 S4+S5) → maintainer sign-off / MS-7

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` (`in_progress`, sign-off pending) · **Slices done**: S4+S5 per plan-001 §3
- **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` §3.4 · **Checkpoint**: `…ms6-cli.checkpoint-004.md`
- **Revision to validate against**: `origin/main` ≥ S4 feat `f357584` + this boundary commit

## Receiver validation checklist (complete BEFORE any edit)

- [ ] `git pull` · `git log --oneline -6` (top: S4 `feat(cli)` + this boundary commit); `git status` clean — records beat briefings; surface stale premises, never code over them
- [ ] `npm ci --cache /tmp/boldash-npm-cache && npm run verify` → exit 0, **319/319** (four stages; never read a gate through a pipe without `${PIPESTATUS[0]}`)
- [ ] Read order: `AGENTS.md` → handoff-004 + checkpoint-004 → `docs/STATE.md` → MS-6 record → `docs/cli-reference.md` §verify + §workflow import → `src/cli/commands/{verify,workflow}.ts` → `src/cli/main.ts` (async dispatch)
- [ ] `gh issue view 6` — claimed, in_progress; #15 (SECURITY email) maintainer-only
- [ ] Recite: P1–P10 · ajv-only · registry honesty (docs now mirror the registry: deferred sections marked) · binding ruling · verify log-only · silence ≠ authorization · per-instance push naming · `date -u`

## Scope accepted by receiver

**Maintainer sign-off on MS-6** (no code remains). Checklist against the record's AC table:

- AC-1: golden tests per command on temp fixture repos, JSON stdout validated, exits per errors.md — evidence: `tests/golden/cli-{init,state-workflow,route-state,verify-import}.test.ts`, 319/319 + CI 34952209416 @ `f357584`
- AC-2: `--format json` no ANSI, stable key order — evidence: `tests/unit/cli/output.test.ts` + golden envelope parses
- AC-3: root help 64/200, per-command ≤40 (verify 16, workflow 30) — evidence: `tests/unit/cli/help.test.ts`
- AC-4: unknown task / bad flags → exit 2 — evidence: golden unknown-id + usage tests per family
- AC-5: gates exercised PASS and BLOCK, no warn-only gate — evidence: golden PASS→BLOCK same-task journey + real timeout 12 + import strip proof
- AC-6: cli-reference in sync (init scaffold-only, :125 four-pack, requirement-add section, verify exit-12, import semantics) — evidence: this boundary's doc diff
- AC-7: exits match errors.md for every command — evidence: golden exit asserts 0/1/2/3/4/12 paths; no new codes shipped
- AC-8: file-baseline capture at planned→implementing — evidence: S3 golden (unchanged by S4/S5)

On sign-off: record → `completed`, close #6, open MS-7 (#7) with its own plan. Do NOT mark complete without the maintainer's explicit word — tests are evidence, not proof.

## Known gaps the receiver must NOT paper over

- `verify --all` empty-set exit 0 is a receiver-side minimal choice (cli-reference/spec silent) — recorded in checkpoint-004 and the verify header; a future RFC may rule otherwise
- `verify` log-only (no auto-transition) is the safest cli-reference reading, not a spec quote — same recording
- Import stub fallback (`evidence_exists` on kind `import`) has no consumer yet — MS-7/8 wire it or replace it; do not claim verify-of-imported-packs works end to end
- `workflow list` still shows only the 4 built-ins after import (registry in-memory until MS-8) — documented in the import report note; do not "fix" by re-reading files in S5 clothing
- getting-started verification example output remains illustrative (per-requirement lines) while the CLI renders the envelope dump — flagged, not rewritten (example-rewrite is MS-7 UX scope)
- npm cache hazard, Prettier⇄freshness loop, PIPESTATUS trap — all in checkpoint-004 §Anomalies
