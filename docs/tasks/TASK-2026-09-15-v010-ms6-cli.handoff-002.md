# Handoff Record 002 — DSH session (MS-6 S2) → fresh session (S3)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` (in_progress) · **Next slice**: S3 per plan-001 §3
- **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` §3.4 (closed v0.1.0 command set)
- **Checkpoint link**: `docs/tasks/TASK-2026-09-15-v010-ms6-cli.checkpoint-002.md`
- **Revision to validate against**: `origin/main` ≥ S2 feat `01c1154` + this boundary commit

## Receiver validation checklist (complete BEFORE any edit)

- [ ] `git pull` · `git log --oneline -6` (top: S2 `feat(cli)` + this boundary commit); `git status` clean — if the record shows states that contradict your briefing, the records win: surface the mismatch, do not code over it
- [ ] `npm ci --cache /tmp/boldash-npm-cache && npm run verify` → exit 0, **271/271** (four stages; if red: STOP, reconcile)
- [ ] Read in order: `AGENTS.md` → this + checkpoint-002 → `docs/STATE.md` → MS-6 record → **plan-001 §3 S3** → `docs/cli-reference.md` §route → `docs/routing-contract.md` → router public surface `src/core/router/index.ts` → **`schemas/done.schema.json` + `src/core/verification/{checks,gate,evidence}.ts`** (how `file_not_modified` resolves evidence — the baseline format must be proven against the consumer before you invent a writer) → `src/cli/commands/{index,state}.ts` (patterns + family-runner precedent)
- [ ] `gh issue view 6` — claimed, in_progress; #12/#14 closed; #15 (SECURITY email) maintainer-only
- [ ] Recite: P1–P10 · ajv-only · registry-honesty (S3: wire `route`/`state transition|requirement|evidence` only when runnable — no pre-registration) · read-bytes-before-write + same-step provenance · silence ≠ authorization · four-stage gate · per-instance push naming

## Scope accepted by receiver

S3 only (plan-001 §3): `route` reading `--input <path>` / `--json '<inline>'` / stdin through the router's `routeValidated`/`parseProposal` surface, envelope budgets **≤80 tokens success / ≤140 failure** asserted by a deterministic serializer test (RFC §1.2, P9); `--create` writes the routed task as `planned` through `TaskStore.create` (events recorded); then `state transition <id> <status>` (guards incl. `STATE_MISSING_REQUIREMENT`), `state requirement add <id>`, `state evidence add <id> --type --ref` — all writes via State Engine, exits 0/2/4. **AC-8**: the `planned→implementing` transition captures file-baseline evidence (path + sha256) for every bound `must_not` path — without it `file_not_modified` can only ever block (MS-5 design). Where `must_not` paths come from and where baseline evidence lands are NOT yet decided — read the consumers first; if genuinely undefined, open an issue and pick the minimal honest behavior (AGENTS §When Uncertain).

## Known gaps the receiver must NOT paper over

- AC-8 baseline capture is MS-6's most consequential behavior: prove it with a test showing `file_not_modified` PASSes with the baseline and BLOCKs on post-baseline modification (PASS-and-BLOCK discipline; a capture that the verifier ignores is ceremony)
- `route` needs stdin — `main.ts` `run()` currently has argv + output streams only; a stdin seam is a kernel touch: justify in the record, keep `run()` injectable for golden tests
- `--create` + optimistic versioning: the created task's `version`/id come from the engine (`TASK-###` auto-id) — echo them in the envelope; `state` write commands must carry `--expect-version`-style input ONLY if cli-reference documents it (it doesn't yet → read task record before adding; S5 docs sync may amend)
- Route proposal shape is validated against `route.schema.json` by the engine (`parseProposal`) — do not re-validate in the CLI
- cli-reference §state get still shows `--format md` (kernel: human|json only) — S2 ruling in record; do not implement md silently; S5 patches the doc
- npm default-cache root-ownership hazard persists (`--cache /tmp/…`); CI watch must assert headSha; never read the gate from piped output
- Help budgets: root help grows with each family (≤200 words) — S3 adds `route`; drop `--quiet`/`--verbose` detail lines first if needed, never the honesty rule
