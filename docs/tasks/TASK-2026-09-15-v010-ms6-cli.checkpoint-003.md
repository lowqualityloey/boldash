# Checkpoint Record 003 — TASK-2026-09-15-v010-ms6-cli (session boundary: S3 → S4)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` · **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` §3.4
- **Checkpoint type**: Session boundary (slice-scoped GO: this instance authorized S3 incl. gate→commit→push per handoff-002 §Scope)
- **Branch / Revision**: `main` @ S3 feat `c539c56` + this boundary commit (push + headSha CI watch at tip)
- **Timestamp**: 2026-09-15 04:24 UTC (`date -u`)

## State

- **MS-6**: `in_progress` · #6 assigned · M2: MS-1…5 ✅ · **S1 ✅ S2 ✅ S3 ✅ / S4–S5 open**
- **Suite**: **301/301** four-stage gate green 04:20 UTC this session (30 new tests over S2's 271)
- **Milestone position**: after S3 the CLI has init · route · state(get/list/transition/requirement add/evidence add) · workflow(list/validate); unwired: verify · workflow import

## Completed this session (S3, plan-001 §3)

- `route` — three proposal sources (`--input`/`--json`/piped stdin, exactly-one enforced), pipeline errors pass through verbatim (SCHEMA_PARSE/SCHEMA_VALIDATION/WORKFLOW_NOT_FOUND/LEVEL_RISK_MISMATCH), documented success payload, P9 budgets **asserted through the real serializer**: ≤80 success / ≤140 failure
- `route --create` — engine create → `proposed → planned`, engine-assigned id/version echoed; refuses proposals lacking `task.summary` instead of inventing titles; precondition-gated
- CLI-owned `route.validated` events (ruling D-3) via new additive `TaskStore.logEvent`; logged only inside an initialized repo (routing elsewhere writes nothing)
- `state transition` / `requirement add` (auto R-ids) / `evidence add` (EvidenceStore + paired `task.evidence_added`) — every guard tested PASS **and** BLOCK; `blocked` requires `--reason` (usage error otherwise) and records `blocked_reason`
- **AC-8 file-baseline capture** at `planned→implementing` — capture-before-transition (escape/corrupt contract refuses, state never advances); bound contract ruled `.boldash/workflows/<task.workflow>/done.schema.json` (RFC §3.2 layout + D1 template purpose); evidence format taken **verbatim from the consumer** (`kind file-baseline`, `path`, `hash sha256:<hex>`, absent ⇒ null); cross-verified through `buildEnv` + `runCheck`: PASS unmodified / BLOCK tampered / BLOCK no-baseline
- Kernel touches (both justified + recorded): declarative `readsStdin` + memoized injected stdin (types/main), public `logEvent` (task-store); third-level verbs (`requirement add`) dispatch on positionals — parser unchanged, honest help via decorative `add` specs
- help.test.ts honesty flipped for `route` (wired now); registry walks budgets for all subcommands

## Remaining work (MS-6)

1. **S4**: `verify <TASK>|--all` → engine `buildEnv/loadContract/verifyTask`; exits **0/1/2/12** (timeout amendment per errors.md); contract discovery must reuse the S3 binding ruling; `--all` scans `verifying` tasks. `workflow import <path>` → schema-validated import (plan-001 S4; RFC §4 row: importer emits **conservative stub contract** — no auto-generated runnable `command` checks — + import report). AC-5: per-gate PASS-and-BLOCK evidence via the CLI surface.
2. **S5**: docs sync (AC-6) + full-surface budgets + record AC fill + sign-off + close #6

## Decisions & invariants (bind the next session)

- Unchanged locked set (checkpoint-002 §Decisions still binds) plus: **contract binding** and capture-before-transition rulings (record §Scope Change S3) — S4 must resolve verify-contracts the same way, no second convention
- `state claim` remains out (RFC §3.4 closed set); cli-reference shows it — S5 doc patch list grows: claim row, `--format md`, :125, verify exit 12
- CONCURRENT_MODIFICATION exit 4: engine-enforced; CLI tests must not fake races (MS-2 owns that branch)
- Read-bytes-before-write + same-step provenance; `date -u`; silence ≠ authorization; per-instance push naming; headSha-asserted CI watch; gates never read through pipes without PIPESTATUS

## Anomalies & lessons (this session — for NOTES §6 mining)

1. **Capture-before-guard order found by the tests themselves**: an orphaned baseline from a blocked transition is benign (consumer key is task+path+hash) — but the reverse order (transition first) would leave implementing-with-no-baseline, exactly the state NOTES §4 forbids. Recorded as ruling; order verified by the escape-contract test (`state never advances`).
2. **Test-placement bug caught by its own suite**: a precondition test inside a `beforeEach(initBoldash)` describe received `STATE_TASK_NOT_FOUND` — the repo it "wasn't initialized in" was initialized. Lesson: precondition tests belong in their own fresh-dir describe (the failure mode was a WRONG-PASS-adjacent lie, not a product bug).
3. Two synthetic-fixture bugs recurred from checkpoint-002 lesson 2 (owner-`null` expectation; `proposed→implementing` misedge in a test) — re-derive expectations from schema/matrix bytes, not memory, every time.
4. `JSON.stringify` output has **no spaces** — `.replace('"level": 2', …)` silently doesn't match; mutation-style test fixtures must assert their mutation took effect (added to S4 checklist).
5. Prettier⇄edit freshness loop recurred (4th milestone running) — batch `--write` immediately before final gate only; between-write edits re-read first.
6. PIPESTATUS discipline held this session: the one piped focused run echoed `EXIT=0` while 1 test had failed — tail output, not `$?`, carried the truth; final gates all used `PIPESTATUS[0]`.

## Blockers

- None. S4 needs no human input. One maintainer-visible ruling to ratify at S5 review: contract binding (record §Scope Change S3-1).

## Exactly one prioritized next action

**S4** (plan-001 §3): `verify TASK|--all` + `workflow import` — before coding, read plan-001 §3 S4 verbatim, cli-reference §verify/§workflow import, `docs/verification-guide.md` §Gates and Exit Codes (incl. the 0/1/2/12 timeout amendment), `src/core/verification/{gate,contract,index}.ts` cold paths, and note the plan invariant that core never parses YAML (config.yaml is written-only) — the v1/PromptKit importer is **MS-8 (#8)**, not S4; S4's import is "schema-validated pack import (exit 0/2/3)" whose pack shape the receiver must pin down from those exact words + the done-schema meta-schema, resolving any YAML tension honestly (record it, don't silently expand scope). Contract discovery MUST reuse the S3 binding (`.boldash/workflows/<workflow>/done.schema.json`). Exit-12 needs a real timing fixture (short `timeout_ms` in a declared command check; gate proven to block). AC-5: every check type PASS-and-BLOCK through the CLI surface itself.
