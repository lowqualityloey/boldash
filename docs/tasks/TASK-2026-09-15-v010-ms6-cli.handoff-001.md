# Handoff Record 001 — DSH session (MS-6 claim + S1) → fresh session (S2)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` (in_progress) · **Next slice**: S2 per plan-001
- **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` §3.4 (closed v0.1.0 command set)
- **Checkpoint link**: `docs/tasks/TASK-2026-09-15-v010-ms6-cli.checkpoint-001.md`
- **Revision to validate against**: `origin/main` ≥ `07191ca` + checkpoint/handoff commit

## Receiver validation checklist (complete BEFORE any edit)

- [ ] `git pull` · `git log --oneline -6` (top: S1 `feat(cli)` + this boundary commit); `git status` clean
- [ ] `npm ci --cache /tmp/boldash-npm-cache && npm run verify` → exit 0, **229/229** (four stages now — format:check included; if red: STOP, reconcile)
- [ ] Read in order: `AGENTS.md` → this + checkpoint-001 → `docs/STATE.md` → MS-6 record → **plan-001 §3 S2** → `src/cli/{argv,output,help,main}.ts` + `src/cli/commands/index.ts` (the kernel you're extending) → `docs/cli-reference.md` §state/§workflow
- [ ] `gh issue view 6` — claimed, in_progress; #12/#14 closed with rulings
- [ ] Recite: P1–P10 · ajv-only · registry-honesty rule (unwired commands absent) · read-bytes-before-write + same-step provenance (checkpoint §Anomalies 8) · silence ≠ authorization · four-stage gate

## Scope accepted by receiver

S2 only (plan-001): `state get/list` (+filters; unknown id → `STATE_TASK_NOT_FOUND` **exit 2** — catalog and cli-reference agree; the plan's earlier "4" was a slip, corrected in plan-001) and `workflow list/validate` (R1 four-pack registry; `validate` semantics question open — checkpoint §"Exactly one"). Then four-stage gate → commit → push only under per-instance naming.

## Known gaps the receiver must NOT paper over

- `workflow validate <name>` has no defined on-disk object to validate until pack format lands (MS-8) — decide in S2 between registry-entry structural validation and a documented stub, with an issue if genuinely undefined
- `workflow list` prints registry packs (4) — cli-reference:125's 5-name line stays until S5 docs sync (don't "fix" it early outside plan)
- Init's briefing file + host auto-detect deferred to MS-7 (R3); cli-reference §init still lists them — S5 reconciles
- S3 carries the file-baseline obligation (AC-8); S4 the PASS-and-BLOCK proof (AC-5) — do not let them silently slide to S5
- npm default-cache root-ownership hazard persists (`--cache /tmp/…`)
- Push/merge/tag boundaries unchanged (#11 ruling); CI watch must assert headSha match (lesson 4)
