# Checkpoint Record 001 — TASK-2026-09-15-v010-ms6-cli (session boundary: S1 → S2)

- **Task ID**: `TASK-2026-09-15-v010-ms6-cli` · **Spec**: `docs/specs/2026-09-15-spec-v0.1.0-foundation.md` (§3.4 = v0.1.0 surface)
- **Checkpoint type**: Session boundary (maintainer-chosen after S1; context endurance exceeded at ~16 turns)
- **Branch / Revision**: `main` @ `07191ca` = origin/main (S1 tip; this checkpoint + handoff land in the next commit)
- **Timestamp**: 2026-09-15 02:04 UTC (`date -u`)

## State

- **MS-6**: `in_progress` · #6 assigned (claimed 01:24 UTC with GO-MS6) · M2 position: **MS-1…MS-5 ✅ · MS-6 S1 ✅ / S2…S5 open · MS-7…MS-9 planned**
- **Suite**: **229/229** four-stage gate (`verify` = lint → format:check → typecheck → test), CI success ×3 matrices (`verify node 20/22/24`) @ `07191ca`, run 34919452217 (headSha-matched)

## Completed this session (governance + S1, all pushed)

- Receiver checklist 5/5; rulings adopted from maintainer: **R1** #12 four-pack canonical (#12 closed), **R2** #14 four-stage gate (folded `acf47e7`, #14 closed), **R3** `init` scaffold-only, **R4** S1–S5 approved (plan-001), **R5** SECURITY → #15
- State Engine `scaffold.ts` (sole sanctioned `.boldash/` writer); registry-driven argv + help (parse/help cannot drift); envelope output (sorted-key JSON, quiet/verbose/color); 2 new catalog codes via errors.md's own add-path (`CLI_USAGE`, `CLI_PRECONDITION_FAILED`); `bin` + `pretest` build wiring
- Golden tests spawn the **built** CLI on temp fixture repos — first end-to-end packaging proof in the repo

## Remaining work (MS-6)

1. **S2**: `state get/list` + `workflow list/validate` (read-only; State Engine + router registry only) — next action
2. **S3**: `route` (+`--create` incl. **file-baseline capture at planned→implementing**, AC-8) + `state transition` + `requirement/evidence add`
3. **S4**: `verify TASK|--all` (exit 12 incl.) + `workflow import` + per-gate PASS-and-BLOCK evidence (AC-5)
4. **S5**: docs sync (AC-6: `cli-reference.md:125` pack line, init scaffold-only wording, root-help example), gate proof, record completion → maintainer sign-off → #6 close

## Decisions & invariants (bind the next session)

- P1–P10 + tie-breaker · ADRs 0001–0005 · **ajv@8.20.0 SOLE runtime dep** (config.yaml is written as literal, never parsed) · exact pins, ESM, strict TS incl. `exactOptionalPropertyTypes` → optional props need conditional spread, not `undefined`
- **Command honesty rule (S1 precedent)**: the registry carries only wired commands; `--help` never advertises unimplemented surface; root help grows as slices land
- RFC §3.4 is the CLOSED v0.1.0 surface — `status/explain/gates/doctor/report/repair/restore` exist in cli-reference but are **out of MS-6 scope** (trim approved in RFC; P10's `explain` tension is ruled away by §3.4, do not re-litigate silently)
- Read bytes before write + post-write absence/presence asserts; `date -u`; silence ≠ authorization; push needs per-instance naming
- Gates: four stages from now on — corpus must stay prettier-clean at every commit

## Anomalies & lessons (this session — for NOTES §6 mining)

1. **False-premise disclosure (mine)**: claimed "CI already runs format:check" in D2 rationale — grep proved it doesn't; corrected on disk. Ruling unaffected; premise claims need byte-checks BEFORE display
2. **#14's own premise had rotted** (all four sources already 3-stage); stale-issue rulings must be re-grounded at adoption time
3. `gh` chained commands with unsupported `-q` silently no-opped the whole `&&` chain — first comment/close batch did nothing; verified retry caught it. **Never trust compound gh one-liners; echo state after**
4. **CI watch race**: `gh run list -L 1` right after push returned the PREVIOUS run — watched a success that wasn't mine. Always assert `headSha` matches the pushed SHA
5. Two golden/unit tests initially asserted MY guess, not the contract (profile-per-invocation; `host:` substring in a comment) — fix tests to truth, never product to test
6. npm default cache still root-owned (known hazard); `npm ci --cache /tmp/…` workaround works, unchanged
7. Edit tool stale-read guard fired once (post-prettier); re-read-then-edit remains the loop
8. **Fabricated provenance caught pre-commit**: checkpoint first draft carried a run ID (34918735174 — never existed) and timestamp reconstructed FROM MEMORY; re-ran `date -u` + `gh run list` and corrected. Rule for all future records: every ID/time/conclusion line must be executed or copy-pasted in the same step, never recalled

## Blockers

- None. S2→S5 need no human input until S5 completion sign-off.

## Exactly one prioritized next action

**S2** (plan-001 §3): build `state get/list` + `workflow list/validate` read-only commands on the S1 kernel — subcommand wiring, filters, `STATE_TASK_NOT_FOUND` (4), registry's 4 packs (R1), per-command help ≤40 tokens. Open starting question for the session: `workflow validate <name>` semantics against registry entries vs. on-disk packs (no pack format exists — likely validate-schema-of-registry-entry; check cli-reference §workflow when S2 starts; if genuinely undefined → issue + minimal honest behavior, per AGENTS §When Uncertain).
